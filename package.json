{
  "name": "catalog",
  "version": "0.0.2",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "./node_modules/.bin/esbuild main.tsx --bundle --format=esm --global-name=pluginModule --external:apyf --outfile=main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apyf": "^1.0.8"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}
